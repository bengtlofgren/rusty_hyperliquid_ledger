# Hyperliquid Network
# Options: mainnet, testnet (default: mainnet)
# Note: hypersdk handles RPC URLs internally
HL_NETWORK=mainnet

# Builder-only mode (optional)
# Set this to filter trades by builder attribution
# TARGET_BUILDER=0x...

# Server Configuration
BIND_ADDR=0.0.0.0:8080

# Logging
# Options: error, warn, info, debug, trace
RUST_LOG=info,hl_server=debug,hl_api=debug,hl_indexer=debug
